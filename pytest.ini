[pytest]
# Born Clean: pure-Python tests live in tests/ and import core modules
# via conftest.py's importlib loader. __init__.py uses lazy bpy imports
# so pytest discovery doesn't crash outside Blender.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --import-mode=importlib
